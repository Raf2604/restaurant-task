//FOR FUTURE: IF WE ADD SOME NEW FUNCTIONALS WE CAN ALSO USE REDUX
//FILE FOR REDUCER

const initState = {};

const reducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
